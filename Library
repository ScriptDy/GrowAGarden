-- SKIDDED BY SIGMA @rizzify101
-- GUI Styled like GAG Script Loader 2.0 (with small "D" logo toggle + Scrollable buttons)

local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- Main GUI
local gui = Instance.new("ScreenGui")
gui.Name = "GAG_ScriptLoader"
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = game:GetService("CoreGui")

-- Main Frame (taller for scrollable space)
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 360, 0, 460) -- 🔹 taller height so scroll line shows
frame.Position = UDim2.new(0.5, -180, 0.5, -230)
frame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
frame.BackgroundTransparency = 0.3
frame.BorderSizePixel = 0
frame.Parent = gui
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 12)

-- Stroke border
local stroke = Instance.new("UIStroke")
stroke.Thickness = 2
stroke.Color = Color3.fromRGB(200, 200, 200)
stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
stroke.Parent = frame

-- Mini Logo (toggle when minimized)
local miniLogo = Instance.new("TextButton")
miniLogo.Size = UDim2.new(0, 50, 0, 50)
miniLogo.Position = UDim2.new(0, 20, 0.8, 0)
miniLogo.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
miniLogo.BorderSizePixel = 0
miniLogo.Text = "D"
miniLogo.TextColor3 = Color3.fromRGB(255, 255, 255)
miniLogo.Font = Enum.Font.GothamBold
miniLogo.TextSize = 28
miniLogo.Visible = false
miniLogo.Parent = gui
Instance.new("UICorner", miniLogo).CornerRadius = UDim.new(1, 0)

-- Topbar
local topbar = Instance.new("Frame")
topbar.Size = UDim2.new(1, 0, 0, 40)
topbar.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
topbar.BorderSizePixel = 0
topbar.Parent = frame
Instance.new("UICorner", topbar).CornerRadius = UDim.new(0, 12)

-- Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -100, 1, 0)
title.Position = UDim2.new(0, 10, 0, 0)
title.BackgroundTransparency = 1
title.Text = "GAG SCRIPT LOADER 2.0"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 18
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = topbar

-- Minimize Button
local minBtn = Instance.new("TextButton")
minBtn.Size = UDim2.new(0, 26, 0, 26)
minBtn.Position = UDim2.new(1, -60, 0.5, -13)
minBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
minBtn.BorderSizePixel = 0
minBtn.Text = "-"
minBtn.Font = Enum.Font.GothamBold
minBtn.TextSize = 22
minBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
minBtn.Parent = topbar
Instance.new("UICorner", minBtn).CornerRadius = UDim.new(0, 6)

-- Exit Button
local exitBtn = Instance.new("TextButton")
exitBtn.Size = UDim2.new(0, 26, 0, 26)
exitBtn.Position = UDim2.new(1, -30, 0.5, -13)
exitBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
exitBtn.BorderSizePixel = 0
exitBtn.Text = "X"
exitBtn.Font = Enum.Font.GothamBold
exitBtn.TextSize = 22
exitBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
exitBtn.Parent = topbar
Instance.new("UICorner", exitBtn).CornerRadius = UDim.new(0, 6)

exitBtn.MouseButton1Click:Connect(function()
    gui:Destroy()
end)

-- Draggable Frame
local dragging, dragStart, startPos
topbar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = frame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)
topbar.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X,
                                   startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Minimize Logic
local minimized = false
minBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    frame.Visible = not minimized
    miniLogo.Visible = minimized
end)
miniLogo.MouseButton1Click:Connect(function()
    minimized = false
    frame.Visible = true
    miniLogo.Visible = false
end)

-- ✅ Scrollable Button Holder (adjusted for new size)
local scrollFrame = Instance.new("ScrollingFrame")
scrollFrame.Size = UDim2.new(1, -10, 1, -60) -- fills under topbar
scrollFrame.Position = UDim2.new(0, 5, 0, 50) -- start just below topbar
scrollFrame.BackgroundTransparency = 1
scrollFrame.BorderSizePixel = 0
scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
scrollFrame.ScrollBarThickness = 8 -- thicker, easier to see
scrollFrame.ScrollBarImageColor3 = Color3.fromRGB(200, 200, 200) -- brighter scrollbar
scrollFrame.Parent = frame

local layout = Instance.new("UIListLayout")
layout.Padding = UDim.new(0, 10)
layout.FillDirection = Enum.FillDirection.Vertical
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
layout.SortOrder = Enum.SortOrder.LayoutOrder
layout.Parent = scrollFrame

layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    scrollFrame.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 10)
end)

-- Button Maker
local function createPanelButton(parent, mainText, subText, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -20, 0, 50)
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    btn.BorderSizePixel = 0
    btn.Text = ""
    btn.Parent = parent
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -10, 0, 25)
    label.Position = UDim2.new(0, 10, 0, 5)
    label.BackgroundTransparency = 1
    label.Text = mainText
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.Font = Enum.Font.GothamBold
    label.TextSize = 16
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = btn

    local sub = Instance.new("TextLabel")
    sub.Size = UDim2.new(1, -10, 0, 20)
    sub.Position = UDim2.new(0, 10, 0, 25)
    sub.BackgroundTransparency = 1
    sub.Text = subText
    sub.TextColor3 = Color3.fromRGB(180, 180, 180)
    sub.Font = Enum.Font.Gotham
    sub.TextSize = 14
    sub.TextXAlignment = Enum.TextXAlignment.Left
    sub.Parent = btn

    btn.MouseButton1Click:Connect(callback)
    return btn
end

-- ✅ Script Buttons (auto-stack with scroll)
createPanelButton(scrollFrame, "Project Reverse", "Click to Execute", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/veryimportantrr/x/refs/heads/main/gag_visual.lua", true))("discord.gg/csxu2nCkw9")
end)

createPanelButton(scrollFrame, "Limit Hub (Keyless)", "Click to Execute", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/FakeModz/LimitHub/refs/heads/main/LimitHub_Luarmor_E.lua"))()
end)

createPanelButton(scrollFrame, "SpeedhubX (Keyless)", "Click to Execute", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/AhmadV99/Speed-Hub-X/main/Speed%20Hub%20X.lua", true))()
end)

createPanelButton(scrollFrame, "Zysume (Keyless)", "Click to Execute", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/ZusumeHub/ZusumeHub/refs/heads/main/UpdateZysume"))()
end)

createPanelButton(scrollFrame, "Egg Randomizer", "Click to Execute", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/KuniDevLua/GrowAGarden/refs/heads/main/EGG%20DETECTOR1"))()
end)

createPanelButton(scrollFrame, "Freeze Trade", "Click to Execute", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/ScriptDy/GrowAGarden/refs/heads/main/FreezeTrade"))()
end)

createPanelButton(scrollFrame, "Pet Age Changer", "Click to Execute", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/ScriptDy/GrowAGarden/refs/heads/main/AgePet"))()
end)

createPanelButton(scrollFrame, "Dark Spawner", "Click to Execute", function()
    local Spawner = loadstring(game:HttpGet("https://gitlab.com/darkiedarkie/dark/-/raw/main/Spawner.lua"))()
    Spawner.Load()
end)
